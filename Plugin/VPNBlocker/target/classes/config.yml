checks:
  # Enable/disable VPN checks on player join
  on-join: true

lists:
  # Download these VPN CIDR lists once on startup/reload and cache on disk
  source-urls:
    - "https://raw.githubusercontent.com/X4BNet/lists_vpn/main/ipv4.txt"
    - "https://raw.githubusercontent.com/MISP/misp-warninglists/main/lists/vpn-ipv6/list.json"
    - "https://www.dan.me.uk/torlist/?exit"
    - "https://raw.githubusercontent.com/Trainboy15/vpn-detector/refs/heads/main/more-ips"
  # Always blocked IPs (exact IP match)
  blacklist: 
    
  # Always allowed IPs (exact IP match, overrides blacklist and VPN list)
  whitelist: []

kick:
  enabled: true
  message:
    - " "
    - " "
    - "              &8[&4Anti-VPN&8]               "
    - "&cVPNs and proxies are not allowed on this server."
    - "&7Please disable your VPN and try again."
  error-message:
    - " "
    - " "
    - "              &8[&4Anti-VPN&8]               "
    - "&cCould not verify your connection."
    - "&7Please try again later."

logging:
  # Log successful checks and API responses
  debug: false
